import ShortFade from './ShortFade'
import SlideFade from './SlideFade'

// This is a MaterialUI transition component, probably would
// rather use the react-spring component, ScrollAnimation
export { ShortFade, SlideFade }
