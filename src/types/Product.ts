export default interface Product {
   id: string
   brand: string
   name: string
   price: number
   quantity: number
   discount: number
   isnew: boolean
   featured: boolean
   images: string[]
   description: string
}
